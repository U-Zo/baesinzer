import React from 'react';
import RoomList from './RoomList';

const Lobby = () => {
  return (
    <div>
      <RoomList roomList={roomList} />
      <button></button>
      <button></button>
    </div>
  );
};

export default Lobby;
